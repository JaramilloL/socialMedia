//crearemos un formulario para que el usuario acceda a la app mediante correo o google

import Login from "../components/loginUser/Login"

const LoginPage = () => {
  return (
    <div>
        <Login/>
    </div>
  )
}

export default LoginPage